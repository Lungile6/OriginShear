{
  "name": "originshear-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001",
    "create-local": "graph create --node http://localhost:8020/ originshear-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ originshear-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
